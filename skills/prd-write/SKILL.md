---
name: prd-write
description: 根据已确认的背景卡、范围和决策账本生成和修订 PRD：CORE 生成目标、范围和核心流程，DETAILS 补充规则、异常、依赖与验收，REVISION 按用户对审核问题的明确处置修订完整 PRD。用户已经通过对应确认点时使用；不用于补造事实或改变未授权决策。
---

# PRD Write

## CORE

1. 确认 CP-P01 已批准可写状态和全部阻塞决策。
2. 读取背景卡、确认后的决策账本和稳定 Context。
3. 仅生成背景、问题、目标、非目标、用户场景、范围、核心流程、确认决策和待确认信息。
4. 不展开字段、异常分支、权限矩阵和完整验收细节。
5. 输出结构化结果及候选 Markdown，等待 CP-P02。

## DETAILS

1. 确认 CP-P02 已批准 CORE 版本、范围和核心流程。
2. 保留已确认主体，补充功能规则、状态、权限、边界、异常、依赖和验收标准。
3. 未核实数据标记“待确认”，不制造量化目标。
4. 按 `schema.json` 返回结果，通过 Harness 写入同一 PRD 路径。

## REVISION

1. 确认上一轮 CP-P03 已选择 `FIX_AND_REVIEW`，并读取当前 PRD、对应审核报告和用户的具体修订决定。
2. 只落实用户明确授权的业务口径，保留未受影响章节，输出完整 PRD Markdown。
3. 沿用稳定 PRD 路径，将当前版本递增一个补丁版本，不复用旧 DETAILS 候选。
4. 写入后必须重新调用 `prd-review`，生成新的审核报告和 CP-P03；REVISION 本身不能直接交付。

## 边界

- 不修改未包含在确认决策中的核心取舍。
- 不写代码、数据库表、接口实现或视觉像素参数。
- DETAILS 不得绕过 CORE 确认。
- REVISION 不得把审核建议本身当成人工决定，也不得扩大修订范围。
- 同一逻辑 PRD 保持稳定路径，通过版本与 Git 历史追踪。

## 资源

- 分阶段规则：`references/stage-rules.md`
- 核心提示：`prompt.md`
- 输出契约：`schema.json`
- 示例：`examples/core-output.json`
